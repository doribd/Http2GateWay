server.port=8443
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-type=PKCS12
server.ssl.key-store-password=qwe123@
server.http2.enabled=true
#server.ssl.enabled-protocols=TLSv1.2
# keystore.p12 generated with:
# openssl req -newkey rsa:2048 -nodes -keyout key.pem -x509 -days 365 -out keystore.pem
# openssl pkcs12 -inkey key.pem -in keystore.pem -export -out keystore.p12
# openssl pkcs12 -in keystore.p12 -noout -info
server.ssl.ciphers=TLS_RSA_WITH_AES_128_CBC_SHA256
server.ssl.protocol=TLS
# server.ssl.enabled=true
